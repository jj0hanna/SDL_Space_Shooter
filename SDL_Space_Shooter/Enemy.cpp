#include "Enemy.h"
#include "SDL.h"
#include "SDL_image.h"
#include <iostream>

using namespace std;
	
//			void Enemy::move(struct Position* p, int count, float move_x, float move_y)
//{
   // for (int i = 0; i < count; ++i) {
   //     p[i].x += move_x;
   //     p[i].y += move_y;
   // }
//}

//void Enemy::draw(struct Body* Body)
//{
//	Body->rect.x = 0;//
//	Body->rect.y = 0;
//	Body->rect.w = 32;
//	Body->rect.h = 32;
//
//	SDL_SetRenderDrawColor(Body->renderer, 25, 25, 40, 255);
//	SDL_Rect rect = { (int)Body->rect.x, (int)Body->rect.y, Body->rect.w, Body->rect.h };

//	SDL_RenderFillRect(Body->renderer, &rect);
	
//}

//void Enemy::draw()
//{
	//SDL_SetRenderDrawColor(renderer, 150, 25, 40, 255);
	//SDL_Rect rect = { (int)x, (int)y, 32, 32 };

	//SDL_RenderFillRect(renderer, &rect);
//}

//void Enemy::Entity(float p_x, float p_y)
//{
//	x = p_x;
//	y = p_y;
//
//	rect.x = 20;
//	rect.y = 20;
//	rect.y = 32;
//	rect.w = 32;
//}
